# -*- python -*-

## Graphite local_settings.py

## most settings in /path/to/settings.py are correct, but there are a few
## tweaks/customizations required.

# Set your local timezone (Django's default is America/Chicago)
# If your graphs appear to be offset by a couple hours then this probably
# needs to be explicitly set to your local timezone.
TIME_ZONE = '<%= scope.lookupvar("graphite::timezone") %>'

# Enable full debug page display on exceptions (Internal Server Error pages)
DEBUG = True

## since we're using a custom path for the whisper files, that must be updated
## to match carbon.conf
WHISPER_DIR = '<%= whisper_data_dir %>'

LOG_DIR = '<%= logroot %>'

DATABASE_NAME = '<%= lib_dir %>/graphite.db'
INDEX_FILE = '<%= lib_dir %>/index'
